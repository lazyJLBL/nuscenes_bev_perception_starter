# MySQL 8.0 connection for the productized admin/client API.
# Do not commit real passwords. Set DATABASE_URL in your shell, process manager, or server secret manager.
DATABASE_URL=mysql+pymysql://root:<password>@localhost:3306/nuscenes_bev_platform?charset=utf8mb4

# Optional comma-separated frontend origins for deployed clients.
FRONTEND_ORIGINS=http://localhost:5174
